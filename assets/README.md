# Brand Assets

Approved assets belong here once source and rights are known. Each family needs METADATA.md listing source master, exports, version, approval date, owner, color space, intended medium, permitted use, and third-party dependencies. Never treat a compressed export as the editable master. Use Git LFS for large binaries.
